doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    title Chick Stock Request-Form 
    // ✅ Bootstrap 5 CSS
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css', rel='stylesheet')
    link(rel='stylesheet', href='/custReg.css')
  body
    // Navbar
    nav.navbar.navbar-expand-lg.navbar-dark.bg-success.sticky-top
      .container-fluid
        a.navbar-brand.fw-bold(href="#") 🐥 Young4ChickS
        button.navbar-toggler(type="button", data-bs-toggle="collapse", data-bs-target="#navbarNav")
          span.navbar-toggler-icon
        #navbarNav.collapse.navbar-collapse
          ul.navbar-nav.ms-auto
            li.nav-item
              a.nav-link(href="") Dashboard
            li.nav-item
              a.nav-link(href="/feedsrequest") Request Feeds
            li.nav-item
              a.nav-link.active(href="/chickrequest") Request Chicks
            li.nav-item
              a.nav-link(href="", onclick="logout()") Logout
    // Container
    .container.mt-5
      h2.form-title.text-center.mb-4 Chick Stock Request Form
      p.form-label.text-center.mb-4
        | This form is used to register new chick stock into the system. Please enter accurate details.
        | Keeping this information up to date ensures that farmers can receive timely and accurate updates
        | on chick availability. Only authorized personnel should fill this form to maintain data accuracy and
        | integrity.
      p Please note: Only registered young farmers are eligible for chicks request.
      // Card Container
      .card.shadow.p-4
        form#chickForm(action="/chickrequest" method="POST")
          // type of farmer
          .mb-3
            label.form-label(for='farmerType') Type of farmer
            select#farmerType.form-select(name="FarmerType" required='')
              option(value='') --Select--
              option(value='starter') Starter
              option(value='returning') Returning
          // Chick Type
          .mb-3
            label.form-label(for='chickType') Type of Chick
            select#chickType.form-select(name="chickType" required='')
              option(value='') --Select--
              option(value='Layers') Layers
              option(value='Broilers') Broilers
          // catergory of chicks
          .mb-3
            label.form-label(for='chickCategory') Category of Chicks
            select#chickCategory.form-select(name="chickCategory" required='')
              option(value='') --Select--
              option(value='Layers') Local
              option(value='Broilers') Exotic
          // Number of Chicks
          .mb-3
            label.form-label(for='numChicks') Number of Chicks
            input#numChicks.form-control(name="numChicks" type='number', required='', min='1')
          // Age of Chicks in Days
          .mb-3
            label.form-label(for='chickAge') Age of Chicks (in days)
            input#chickAge.form-control( name="chickAge" type='number', required='', min='0')
          // Date Added to Stock
          .mb-3
            label.form-label(for='dateAdded') Date Added to Stock
            input#dateAdded.form-control(name="dateAdded" type='date', required='')
          // Submit Button
          button.btn.btn-success.w-100(type='submit') Make Order
          // Success Message
          p#resultMessage.success.text-center.mt-3
    // Bootstrap JS Bundle
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js')
    // JavaScript
    //script(src='chickstockReq.js')